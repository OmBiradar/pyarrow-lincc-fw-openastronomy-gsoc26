# Parquet reader parallatization

I solved for the reader using parallel threads to read the data,
but never solved the parallelization of the IO. Thus, now I will
need to go through the whole process of reading a file in arrow
to determine all the points of parallalization so that we get the
most performance out of the system.